# Simulator: use localhost. Physical device: use your Mac's local WiFi IP.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080

# Replace with a real Supabase JWT once auth is implemented
EXPO_PUBLIC_AUTH_TOKEN=

# Supabase project config — get from: app.supabase.com → Project Settings → API
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
